FROM node:20-alpine

WORKDIR /usr/src/app

RUN apk add --no-cache \
    python3 \
    make \
    g++ \
    libc6-compat

RUN npm install -g nodemon ts-node tsconfig-paths

# Copiar package.json del proyecto raíz y del servicio
COPY package*.json ./
COPY tsconfig.paths.json ./
COPY microservices/cart/package*.json ./
COPY microservices/cart/tsconfig.json ./
COPY microservices/cart/tsconfig.base.json ./

# Instalar dependencias del proyecto raíz primero
RUN npm install --legacy-peer-deps

# Copiar código fuente del servicio cart
COPY microservices/cart .

# Copiar directorio shared desde el raíz
COPY shared ./shared

RUN addgroup -S appgroup && adduser -S appuser -G appgroup
RUN chown -R appuser:appgroup /usr/src/app
USER appuser

EXPOSE 3102

CMD ["npm", "run", "start:dev"]
