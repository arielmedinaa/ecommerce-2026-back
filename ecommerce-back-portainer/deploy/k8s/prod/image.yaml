apiVersion: apps/v1
kind: Deployment
metadata:
  name: image-service
  namespace: ecommerce-prod
  labels:
    app: image-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: image-service
  template:
    metadata:
      labels:
        app: image-service
    spec:
      containers:
      - name: image-service
        image: your-account-id.dkr.ecr.us-east-1.amazonaws.com/image-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 4093
        envFrom:
        - configMapRef:
            name: ecommerce-config
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: image-service
  namespace: ecommerce-prod
spec:
  selector:
    app: image-service
  ports:
    - protocol: TCP
      port: 3107
      targetPort: 4093
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: image-service-hpa
  namespace: ecommerce-prod
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: image-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
