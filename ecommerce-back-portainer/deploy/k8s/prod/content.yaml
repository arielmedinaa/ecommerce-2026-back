apiVersion: apps/v1
kind: Deployment
metadata:
  name: content-service
  namespace: ecommerce-prod
  labels:
    app: content-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: content-service
  template:
    metadata:
      labels:
        app: content-service
    spec:
      containers:
      - name: content-service
        image: your-account-id.dkr.ecr.us-east-1.amazonaws.com/content-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3103
        envFrom:
        - configMapRef:
            name: ecommerce-config
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: content-service
  namespace: ecommerce-prod
spec:
  selector:
    app: content-service
  ports:
    - protocol: TCP
      port: 3103
      targetPort: 3103
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: content-service-hpa
  namespace: ecommerce-prod
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: content-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
